# Docker workflow disabled - using manual ZIP deployment on justrunmy.app
# Re-enable this file if switching to Docker-based deployment
name: Disabled
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled"
